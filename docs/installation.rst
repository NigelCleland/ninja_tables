============
Installation
============

At the command line::

    $ easy_install ninja_tables

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ninja_tables
    $ pip install ninja_tables